name: Manual – SonarQube Full Scan

on:
  workflow_dispatch:
    inputs:
      branch:
        description: 'Branch to scan'
        required: false
        default: 'master'
        type: choice
        options:
          - master
          - dev

permissions:
  contents: read
  pull-requests: read  # Required by reusable-scan-sonarqube.yml

jobs:
  sonarqube-scan:
    uses: ./.github/workflows/reusable-scan-sonarqube.yml
    permissions:
      contents: read
      pull-requests: read
    with:
      # Reusable auto-detects scan mode: workflow_dispatch event → branch analysis (full scan).
      branch: ${{ inputs.branch }}
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
      SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL }}
